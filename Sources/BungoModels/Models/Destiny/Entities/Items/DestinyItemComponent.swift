// This File was auto generated by BungoCodeGen and should not be modified.
//
// File: DestinyItemComponent.swift

import Foundation

public extension Destiny.Entities.Items {
    /// The base item component, filled with properties that are generally useful to know in any item request or that don't feel worthwhile to put in their own component.
    struct DestinyItemComponent: Codable {
        /// If the item is bound to a location, it will be specified in this enum.
        public var bindStatus: Destiny.ItemBindStatus?

        /// The hash identifier for the specific inventory bucket in which the item is located.
        public var bucketHash: UInt32?

        /// If the item can expire, this is the date at which it will/did expire.
        public var expirationDate: Date?

        /// If this is true, the object is actually a "wrapper" of the object it's representing. This means that it's not the actual item itself, but rather an item that must be "opened" in game before you have and can use the item.
        ///  Wrappers are an evolution of "bundles", which give an easy way to let you preview the contents of what you purchased while still letting you get a refund before you "open" it.
        public var isWrapper: Bool?

        /// The identifier for the item's definition, which is where most of the useful static information for the item can be found.
        public var itemHash: UInt32?

        /// If the item is instanced, it will have an instance ID. Lack of an instance ID implies that the item has no distinct local qualities aside from stack size.
        public var itemInstanceId: Int64?

        /// If available, a list that describes which item values (rewards) should be shown (true) or hidden (false).
        public var itemValueVisibility: [Bool]?

        /// An easy reference for where the item is located. Redundant if you got the item from an Inventory, but useful when making detail calls on specific items.
        public var location: Destiny.ItemLocation?

        /// If the item can be locked, this will indicate that state.
        public var lockable: Bool?

        /// The identifier for the currently-selected metric definition, to be displayed on the emblem nameplate.
        public var metricHash: UInt32?

        /// The objective progress for the currently-selected metric definition, to be displayed on the emblem nameplate.
        public var metricObjective: Destiny.Quests.DestinyObjectiveProgress?

        /// If populated, this is the hash of the item whose icon (and other secondary styles, but *not* the human readable strings) should override whatever icons/styles are on the item being sold.
        /// If you don't do this, certain items whose styles are being overridden by socketed items - such as the "Recycle Shader" item - would show whatever their default icon/style is, and it wouldn't be pretty or look accurate.
        public var overrideStyleItemHash: UInt32?

        /// The quantity of the item in this stack. Note that Instanced items cannot stack. If an instanced item, this value will always be 1 (as the stack has exactly one item in it)
        public var quantity: Int32?

        /// A flags enumeration indicating the transient/custom states of the item that affect how it is rendered: whether it's tracked or locked for example, or whether it has a masterwork plug inserted.
        public var state: Destiny.ItemState?

        /// If this is populated, it is a list of indexes into DestinyInventoryItemDefinition.tooltipNotifications for any special tooltip messages that need to be shown for this item.
        public var tooltipNotificationIndexes: [Int32]?

        /// If there is a known error state that would cause this item to not be transferable, this Flags enum will indicate all of those error states. Otherwise, it will be 0 (CanTransfer).
        public var transferStatus: Destiny.TransferStatuses?

        /// The version of this item, used to index into the versions list in the item definition quality block.
        public var versionNumber: Int32?

        public enum CodingKeys: String, CodingKey {
            case bindStatus
            case bucketHash
            case expirationDate
            case isWrapper
            case itemHash
            case itemInstanceId
            case itemValueVisibility
            case location
            case lockable
            case metricHash
            case metricObjective
            case overrideStyleItemHash
            case quantity
            case state
            case tooltipNotificationIndexes
            case transferStatus
            case versionNumber
        }

        public init(from decoder: Decoder) throws {
            let container: KeyedDecodingContainer<CodingKeys> = try decoder.container(keyedBy: CodingKeys.self)

            bindStatus = try? container.decode(Destiny.ItemBindStatus.self, forKey: CodingKeys.bindStatus)
            bucketHash = try? container.decode(UInt32.self, forKey: CodingKeys.bucketHash)
            expirationDate = try? container.decode(Date.self, forKey: CodingKeys.expirationDate)
            isWrapper = try? container.decode(Bool.self, forKey: CodingKeys.isWrapper)
            itemHash = try? container.decode(UInt32.self, forKey: CodingKeys.itemHash)
            let itemInstanceId = try? container.decode(String.self, forKey: CodingKeys.itemInstanceId)
            if let itemInstanceId, let itemInstanceIdInt = Int64(itemInstanceId) {
                self.itemInstanceId = itemInstanceIdInt
            } else {
                self.itemInstanceId = nil
            }
            itemValueVisibility = try? container.decode([Bool].self, forKey: CodingKeys.itemValueVisibility)
            location = try? container.decode(Destiny.ItemLocation.self, forKey: CodingKeys.location)
            lockable = try? container.decode(Bool.self, forKey: CodingKeys.lockable)
            metricHash = try? container.decode(UInt32.self, forKey: CodingKeys.metricHash)
            metricObjective = try? container.decode(Destiny.Quests.DestinyObjectiveProgress.self, forKey: CodingKeys.metricObjective)
            overrideStyleItemHash = try? container.decode(UInt32.self, forKey: CodingKeys.overrideStyleItemHash)
            quantity = try? container.decode(Int32.self, forKey: CodingKeys.quantity)
            state = try? container.decode(Destiny.ItemState.self, forKey: CodingKeys.state)
            tooltipNotificationIndexes = try? container.decode([Int32].self, forKey: CodingKeys.tooltipNotificationIndexes)
            transferStatus = try? container.decode(Destiny.TransferStatuses.self, forKey: CodingKeys.transferStatus)
            versionNumber = try? container.decode(Int32.self, forKey: CodingKeys.versionNumber)
        }
    }
}
