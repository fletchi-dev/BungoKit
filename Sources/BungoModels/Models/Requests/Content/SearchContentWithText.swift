// This File was auto generated by BungoKitGen
//
// File: SearchContentWithText.swift

import Foundation

public extension Requests.Content {
    /// Gets content based on querystring information passed in. Provides basic search and text search capabilities.
    struct SearchContentWithTextRequest: BungoRequest {
        public typealias Response = SearchResultOfContentItemPublicContract

        public struct Parameters: BungoRequestParameters {
            public var query: [String: String]
            public var path: [String: String]

            public init(locale: String, ctype: String? = nil, currentpage: Int32? = nil, head: Bool? = nil, searchtext: String? = nil, source: String? = nil, tag: String? = nil) {
                query = [:]
                path = [:]

                if let ctype {
                    query["ctype"] = "\(ctype)"
                }
                if let currentpage {
                    query["currentpage"] = "\(currentpage)"
                }
                if let head {
                    query["head"] = "\(head)"
                }
                if let searchtext {
                    query["searchtext"] = "\(searchtext)"
                }
                if let source {
                    query["source"] = "\(source)"
                }
                if let tag {
                    query["tag"] = "\(tag)"
                }

                path["locale"] = "\(locale)"
            }
        }

        public let path = "/Content/Search/{locale}/"
        public let parameters: Parameters
        public let method: HTTPMethod = .get
        public let successStatus: Int = 200

        public init(parameters: Parameters) {
            self.parameters = parameters
        }
    }
}
